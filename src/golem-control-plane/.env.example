# Secrets only — all non-secret configuration lives in config.yaml.
# Copy this file to .env and fill in real values — never commit .env.

# ---------------------------------------------------------------------------
# WatsonX API key — injected into every agent pod at provisioning time
# ---------------------------------------------------------------------------
WATSONX_API_KEY=your-ibm-cloud-api-key-here   # pragma: allowlist secret
